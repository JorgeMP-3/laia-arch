"""AGORA Control Center v2 — async Textual TUI.

Entry point: ``python -m ctl``.

The package replaces the curses TUI in
``infra/dev/agora-control-center-tui.py`` step-by-step over phases A-E.
Until parity is reached the curses TUI stays available as a fallback.
"""

__version__ = "0.1.0"
