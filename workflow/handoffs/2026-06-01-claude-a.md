# Handoff — sesión 2026-06-01 (claude-a, rol Lead)

> El handoff **canónico** de LAIA es la memoria en git (ya actualizada esta sesión):
> `workflow/changelog.md`, `workflow/problems.md`, `workflow/security.md`,
> `workflow/_inbox/roadmap-v2-to-production.md`. Este fichero es solo el resumen consolidado.
> `main` @ `69610fc3` (tras PR #50).

## Qué se hizo (todo mergeado a `main`)

| PR | Qué |
|---|---|
| #46 | Hardening P0 verificado en vivo: SSH (`passwordauthentication/permitrootlogin no`, `allowusers laia-arch`), UFW (deny incoming, solo LAN+tailscale+puentes), permisos secretos 0600/0700. |
| #47 | Fase 1 pre-prod cerrada: 5/7 follow-ups ya estaban; arreglados 1.6 (`preflight.sh`/`smoke-test.sh` → `/srv/laia/state` v2) y 1.7 (`watchdog` en `.laia-core/pyproject.toml`). |
| #48 | `laiactl` reconciliado al **cerebro centralizado** (post-64ba0c2e): install-runtime/init-workspace/init-profile → no-op que verifican el ejecutor; verify/status/control/fleet + agora-backend `get_agent_status` → `laia-executor.service` + `/health` 9091. **Desatascó el alta de usuarios** (agora-backend llamaba a install-runtime y fallaba). |
| #49 | **Fix idmap zona de datos**: `create-agent.sh` chowneaba a `100000` pero la base real es `1000000` → el ejecutor no podía escribir en home/workspace/plugins. Fix = los 3 disk devices con `shift=true`. |
| #50 | Docs de la remediación. |

**Remediación en vivo (HITL Jorge)**: `agent-jorge-dev`, `agent-verify-bob`, `agent-verify-carol` →
`shift=true` + restart. Verificado: `/home/user` root:root y escribible en los 3.

**Server**: reboot a kernel `7.0.0-22` confirmado; primer backup real + snapshots `pre-reboot-*` hechos.

**Validación (VM `laia-dev`)**: suite e2e destructiva **pass=3 fail=0** (T3 golden + T6 load + cutover_regression).
Eran los e2e que nunca se habían corrido — ahora son red de seguridad real y verde.

**Higiene git**: archivadas+borradas 7 ramas mergeadas (bundle en `~/archive/merged-wip-branches-2026-06-01.bundle`).

## Estado del proyecto V2 → prod

Verde: seguridad P0, Fase 1, tooling de provisioning, **agentes escriben en su zona de datos**, red de tests e2e.

**Único hito grande que queda: la VENTANA DE MIGRACIÓN v1→v2 (Fase 2)** — HITL Jorge + Lead.
Runbook: `workflow/plans/estabilizacion/c3-migration-runbook.md` + `workflow/plans/2026-05-31-prod-cutover-v1v2-redesigned.md`.
El host de prod sigue en v1 / era-Hermes `v0.11.0`; v0.2.0 está en `stable`.

## Abierto / deuda (no bloqueante de la ventana)

- `problems.md`: `create-agora.sh:53` usa `chown 100000` para el dir del container AGORA — revisar (AGORA funciona hoy, baja prioridad).
- Comandos `set-agent-persona/instructions/skill` + `get/update_profile` de agora-backend siguen contra el runtime archivado → reescribir contra `agora.db` (deuda del modelo centralizado).
- `ui-prod-layer-rota-v0.11.0` (open): la consola/UI de prod necesita remake (Fase 7).
- Rotación precautoria de tokens OAuth + password admin Nextcloud (P0.4) — decisión de Jorge, no bloqueante.
- Bug de prod sin verificar: `agora.db` test pool / T3-T6 ya cubiertos.

## Suggested skills para la próxima sesión

- **`grill-me` / `grill-with-docs`** → antes de la ventana de migración (Fase 2), interrogar el runbook del cutover contra el estado real ya migrado del tooling.
- **`diagnose`** → si la ventana destapa algo (mismo patrón que hoy con T3).
- **`verify`** → tras el cutover, smoke e2e (login + chat PA + tool-call + chat LAIA) en prod.
- **`handoff`** → al cerrar la sesión de la ventana.

## Avisos para el próximo agente

- **HEAD compartido de `~/LAIA`** entre IAs: usa **1 worktree por tarea** (esta sesión el HEAD se movió solo a mitad). Ver changelog.
- La VM `laia-dev` vive en `/mnt/data` (HDD mecánico) → provisionar containers es lento (~min), no es CPU. Correr e2e como unidad systemd detached (`systemd-run --unit=...`) + sondear, no `nohup` (muere con la sesión `lxc exec`).
- Para correr e2e en la VM: worktree desde `FETCH_HEAD` (`origin/main` no está como ref local en la VM, clon con refspec limitado).
